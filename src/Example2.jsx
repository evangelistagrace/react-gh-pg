const Example2 = ({onShow}) => {
    return (
        <div>
            <button onClick={() => onShow('Hello supporters!')}>Show total support received</button>
        </div>
    )
}

export default Example2;